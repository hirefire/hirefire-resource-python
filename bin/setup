#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

bin/services up

mise install
mise exec -- pip install poetry
mise exec -- poetry self add poetry-plugin-shell
mise exec -- poetry install
mise exec -- poetry env use python3
